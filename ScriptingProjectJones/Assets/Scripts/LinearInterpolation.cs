using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinearInterpolation : MonoBehaviour
{
/*
    // In this case the result = 4
    float result = Mathf.Lerp(3f, 5f, 0.5f);

    //The result of this line would be 75% of each of the vector's value
    Vector3 from = new Vector3(1f, 2f, 3f);
    Vector3 to = new Vector3(5f, 6f, 7f);

    // Result here = (4, 5, 6)
    Vector3 result = Vector3.Lerp(from, to, 0.75f);

    void Update()
    {
        // The light intensity will change quickly but slows as is gets closer to its target
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f);

        // The light intesity rate changes based on the time instead of the frame
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
    }
*/
}